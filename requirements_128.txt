# MICA requirements for Python 3.11 / PyTorch 2.9.1 / CUDA 12.8.
#
# This pin list mirrors MTamon/FlashAvatar's requirements_128.txt so both
# projects share the same known-good base library versions (in turn aligned
# with MTamon/DECA's CUDA 12.8 branch).
#
# On top of the shared base, MICA needs a small number of extra packages:
#   - insightface                : ArcFace identity backbone + face detection
#   - onnx / onnxruntime-gpu     : insightface model runtime (CUDA provider)
#   - trimesh                    : mesh I/O for demo/utils/masking
#   - tensorboard                : optional training summaries (trainer.py)
#   - gdown                      : pretrained-model download helper (install.sh)
#
# Install via ./install_128.sh (which uses `pip install --no-deps` for the
# shared base to prevent transitive resolution from mutating the pin set).
#
# --- DECA128 / FlashAvatar shared pins (do not drift without checking) ---
Cython==0.29.35
face-alignment==1.4.1
filelock==3.20.0
fsspec==2025.10.0
fvcore==0.1.5.post20221221
ImageIO==2.37.2
iopath==0.1.10
Jinja2==3.1.6
joblib==1.5.2
kornia==0.8.2
kornia_rs==0.1.10
lazy_loader==0.4
llvmlite==0.45.1
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.5
ninja==1.13.0
numba==0.62.1
numpy==2.2.6
nvidia-cublas-cu12==12.8.4.1
nvidia-cuda-cupti-cu12==12.8.90
nvidia-cuda-nvrtc-cu12==12.8.93
nvidia-cuda-runtime-cu12==12.8.90
nvidia-cudnn-cu12==9.10.2.21
nvidia-cufft-cu12==11.3.3.83
nvidia-cufile-cu12==1.13.1.3
nvidia-curand-cu12==10.3.9.90
nvidia-cusolver-cu12==11.7.3.90
nvidia-cusparse-cu12==12.5.8.93
nvidia-cusparselt-cu12==0.7.1
nvidia-nccl-cu12==2.27.5
nvidia-nvjitlink-cu12==12.8.93
nvidia-nvshmem-cu12==3.3.20
nvidia-nvtx-cu12==12.8.90
opencv-python==4.12.0.88
packaging==25.0
pillow==12.0.0
portalocker==3.2.0
PyYAML==6.0.3
scikit-image==0.25.2
scikit-learn==1.7.2
scipy==1.16.3
six==1.17.0
sympy==1.14.0
tabulate==0.9.0
termcolor==3.2.0
threadpoolctl==3.6.0
tifffile==2025.10.16
torch==2.9.1
torchvision==0.24.1
tqdm==4.67.1
triton==3.5.1
typing_extensions==4.15.0
yacs==0.1.8
#
# --- FlashAvatar-aligned extra (used by MICA too) ---
# Logger used across MICA (trainer, tester, flame, demo, ...).
loguru==0.7.3
#
# --- MICA-only additions (not in FlashAvatar / DECA128) ---
# ArcFace identity encoder + face detection (demo, tester, dataset creation).
insightface==0.7.3
# insightface's ONNX model runtime with CUDA 12 provider support.
onnx==1.17.0
onnxruntime-gpu==1.22.0
# Mesh I/O (demo export, utils/masking).
trimesh==4.5.3
# Optional but referenced by micalib/trainer.py when cfg.train.write_summary.
tensorboard==2.18.0
# Used by install.sh to pull FLAME / insightface models from Google Drive.
gdown==5.2.0
#
# --- Built from source by install_128.sh (do not pin here) ---
# - chumpy           : pip install git+https://github.com/mattloper/chumpy.git
# - pytorch3d v0.7.8 : pip install "git+https://github.com/facebookresearch/pytorch3d.git@v0.7.8"
